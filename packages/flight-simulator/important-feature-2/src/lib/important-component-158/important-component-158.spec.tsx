import { render } from '@testing-library/react';

import ImportantComponent158 from './important-component-158';

describe('ImportantComponent158', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<ImportantComponent158 />);
    expect(baseElement).toBeTruthy();
  });
});
