import { render } from '@testing-library/react';

import ImportantComponent157 from './important-component-157';

describe('ImportantComponent157', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<ImportantComponent157 />);
    expect(baseElement).toBeTruthy();
  });
});
